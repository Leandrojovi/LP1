#include <stdio.h>

int main (void) {
	int n;
	int i=0;
	printf("O valor da soma eh: %d");
		scanf("%d", &n);
	while(i<=n){
		n+=i;
		i+=1
	}
	printf("O valor da soma eh: %d", &n);
	
	return 0;
}
